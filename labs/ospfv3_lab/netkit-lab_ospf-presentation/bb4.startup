

ifconfig eth1 up
ifconfig eth1 add 2001:DB8:cad0::4/48


insmod sch_htb

tc qdisc del dev eth1
tc qdisc add dev eth1 root handle 1: htb default 1 
tc class add dev eth1 parent 1: classid 1:1 htb rate 200bit

# Active ipv6 forwarding 
sysctl -w net.ipv6.conf.all.forwarding=1

#/etc/init.d/zebra start

